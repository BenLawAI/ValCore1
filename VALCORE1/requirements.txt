# VALCORE1 Complete Requirements
# This combines both Client Brain and Server Brain dependencies

# ============================================================================
# CORE DEPENDENCIES (Both Client and Server)
# ============================================================================

# Deep Learning
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Data Science
numpy>=1.24.0
scipy>=1.10.0

# HTTP & Networking
requests>=2.31.0
urllib3>=2.0.0
flask>=3.0.0
flask-cors>=4.0.0

# Data Validation
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0
PyYAML>=6.0.0

# System Monitoring
psutil>=5.9.0
pynvml>=11.5.0

# ============================================================================
# CLIENT BRAIN DEPENDENCIES (Desktop - Windows)
# ============================================================================

# Voice Processing
faster-whisper>=0.10.0
openai-whisper>=20230314  # Fallback

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.0
pyaudio>=0.2.13  # Alternative audio backend

# Text-to-Speech
pyttsx3>=2.90  # Primary TTS for fast, responsive voice output
# TTS>=0.13.0  # Optional: Coqui TTS as alternative

# Wake Word Detection
# pvporcupine>=2.2.0  # Requires Picovoice access key

# Speaker Verification
resemblyzer>=0.1.1.dev0

# Desktop Automation
pyautogui>=0.9.54
pillow>=10.0.0
pytesseract>=0.3.10  # OCR support
keyboard>=0.13.5
pygetwindow>=0.0.9

# System Tray
pystray>=0.19.5

# Windows-Specific
pywin32>=306; sys_platform == 'win32'

# ============================================================================
# SERVER BRAIN DEPENDENCIES (ATOM - Linux)
# ============================================================================

# LLM Interface
ollama>=0.1.0  # Ollama Python client

# Vector Database
faiss-cpu>=1.7.4  # Use faiss-gpu if CUDA available on server

# Embeddings
sentence-transformers>=2.2.0
transformers>=4.30.0

# NLP
nltk>=3.8.0
spacy>=3.6.0

# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================

# Development
ipython>=8.12.0
jupyter>=1.0.0
black>=23.0.0
flake8>=6.0.0
pytest>=7.4.0

# Logging & Monitoring
loguru>=0.7.0
tqdm>=4.65.0

# Database (if needed for future expansion)
sqlalchemy>=2.0.0

# ============================================================================
# NOTES
# ============================================================================

# 1. Install CUDA Toolkit 12.1+ for GPU support
# 2. Porcupine requires API key from picovoice.ai
# 3. Ollama should be installed separately on ATOM server
# 4. Some packages may require system libraries:
#    - sounddevice: libportaudio2
#    - pytesseract: tesseract-ocr
# 5. For Windows: Visual C++ Redistributable may be required
# 6. For optimal performance, use pip with --no-cache-dir for large packages

# ============================================================================
# INSTALLATION COMMANDS
# ============================================================================

# Desktop (Windows):
# pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu121

# Server (Linux):
# pip install -r requirements.txt

# Verify GPU:
# python -c "import torch; print(torch.cuda.is_available())"
